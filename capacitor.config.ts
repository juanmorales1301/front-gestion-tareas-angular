import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.juan.app',
  appName: 'front-angular-capacitor',
  webDir: 'dist/front-angular-capacitor/browser',
  bundledWebRuntime: false
};

export default config;
